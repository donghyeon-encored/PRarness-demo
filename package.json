{
  "name": "prarness-demo",
  "version": "1.0.0",
  "description": "A recipe-scaler mock repository for exercising PRarness.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "lint": "npm run lint:app",
    "lint:app": "node --check src/components/recipe-scaler.mjs && node --check src/server.mjs && node --check public/app.mjs && node --check test/recipe-scaler.test.mjs && node --check test/server.test.mjs",
    "test": "npm run test:app",
    "test:app": "node --test test/*.test.mjs"
  }
}
